/**
 * @file ToolsMenu.style.js
 * Exports ToolsMenu component styles.
 */

export default theme => ({
  toolButton: {
    display: 'block',
    marginTop: theme.spacing.unit * 2,
    textAlign: 'center',
    paddingTop: theme.spacing.unit * 2
  }
});
